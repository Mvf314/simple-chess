Implement Unicode symbols. The thing is, they do not fit into a char. So figure out what data type to use.

Ensure shared_ptr does not get copied needlessly. Related: for (const auto& x : list)
Actually, change to a unique_ptr-based system and pass around raw pointers or references if we want non-owning use of members.

Better announcements for check, and game state control flow (maybe don't start immediately, choose what side to play etc.)

Functionally, still need:
playing as black
Pawn promotion
resignation

write a readme

I will add a UI with FTXUI. this should improve control/game flow. Still want it to run in the terminal.

improve the opponent. In the future, maybe some sort of scripting language to describe actions.

Separate logical part (Game) and UI (Window) better. Something like:
Game::sendMove(Move m); (executeMove, this is maybe already okay.)
map out connections between Window and Game class and look to minimize the amount. Also definitely limit access to game state by public member.

Add proper scrolling to the history bar.
